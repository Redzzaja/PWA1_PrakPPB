# oxlint

- **oxlint** for linting, config in `.oxlintrc.json`.
- Run with `npm run lint`.
- Plugins: `react`, `oxc`.
- Rules: `react/rules-of-hooks` (error), `react/only-export-components` (warn, constant exports allowed).

Keep the config minimal; add rules only when they catch a real problem.
